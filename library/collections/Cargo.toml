[package]
name = "libakarin-collections"
version = "0.1.0"
edition = "2024"

[dependencies]
hashbrown = "0.16.1"
thiserror = { version = "2.0.18", default-features = false }

[dev-dependencies]
criterion = { version = "0.6.0", features = ["html_reports"] }
